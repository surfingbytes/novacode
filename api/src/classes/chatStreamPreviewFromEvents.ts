/**
 * Canonical implementation lives in @novacode/shared
 * (shared/src/chatStreamPreview.ts). This shim keeps existing imports working.
 */
export { extractStreamNotificationPreview } from '@novacode/shared';
